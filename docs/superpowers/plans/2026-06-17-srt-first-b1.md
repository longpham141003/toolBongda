# SP1 — SRT-first ở Bước 1: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Đưa phụ đề SRT lên thành sản phẩm chính của Bước 1 — tự tách từ kịch bản, sửa được từng dòng, lưu thành artifact canonical (`scripts/subtitle.json` + `scripts/subtitle.srt`) làm nguồn cho các bước sau.

**Architecture:** Thêm một module lưu trữ phụ đề thuần (`app/pipeline/subtitle_store.py`) chịu trách nhiệm chuẩn hoá + ghi/đọc `subtitle.json`/`subtitle.srt`. Web server thêm endpoint `POST /api/projects/subtitle` để lưu phụ đề đã chỉnh và bổ sung `has_subtitle` + `subtitle_segments` vào payload project. Frontend biến SRT preview (đang read-only) trong `ScriptStepScreen` thành trình sửa được, và lưu canonical khi bấm "Lưu & chọn giọng".

**Tech Stack:** Python 3 + FastAPI + Pydantic (backend), pytest (test), React + Vite (frontend, `webui/`).

## Global Constraints

- SRT canonical lưu tại `<project>/scripts/subtitle.json` (cấu trúc) và `<project>/scripts/subtitle.srt` (xuất ra).
- Cấu trúc mỗi dòng trong `subtitle.json`: `{ "index": int, "start": float (giây), "end": float (giây), "text": str, "edited": bool }`.
- Tách câu dùng đúng quy ước có sẵn: WPS `vi≈2.3, en≈2.6, en-gb≈2.6`, mặc định `2.4`; pause giữa câu `0.32s` (khớp `srt_preview` trong `app/web/web_server.py`).
- File mã hoá UTF-8. Đọc lại JSON dùng `utf-8-sig` (khớp pattern hiện có trong `_project_payload`).
- Tái dùng `build_srt_from_segments` từ `app/voice/text_to_voice_cli.py` để sinh chuỗi `.srt` (không viết lại serializer).
- Không động vào logic voice/prompt trong SP1.

---

### Task 1: Module lưu trữ phụ đề (`subtitle_store`)

**Files:**
- Create: `app/pipeline/subtitle_store.py`
- Test: `tests/test_subtitle_store.py`

**Interfaces:**
- Consumes: `build_srt_from_segments(segments: list[dict]) -> str` từ `app/voice/text_to_voice_cli.py` (đã tồn tại; trả chuỗi SRT từ list segment có `text`/`start`/`end`).
- Produces:
  - `normalize_subtitle_segments(raw: list[dict]) -> list[dict]` — chuẩn hoá, đánh lại `index` tuần tự từ 1, bỏ dòng rỗng, ép `end > start`.
  - `subtitle_paths(project: Path) -> tuple[Path, Path]` — trả `(json_path, srt_path)`.
  - `save_subtitle(project: Path, raw: list[dict]) -> list[dict]` — ghi `subtitle.json` + `subtitle.srt`, trả danh sách đã chuẩn hoá.
  - `load_subtitle(project: Path) -> list[dict]` — đọc `subtitle.json`, trả `[]` nếu không có/đọc lỗi.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_subtitle_store.py
import json
from pathlib import Path

from app.pipeline.subtitle_store import (
    load_subtitle,
    normalize_subtitle_segments,
    save_subtitle,
    subtitle_paths,
)


def test_normalize_reindexes_and_drops_empty():
    raw = [
        {"start": 0.0, "end": 2.0, "text": "  Câu một  "},
        {"start": 2.0, "end": 2.0, "text": ""},          # rỗng -> bỏ
        {"start": 2.0, "end": 1.0, "text": "Câu hai", "edited": True},  # end<=start -> ép tăng
    ]
    out = normalize_subtitle_segments(raw)
    assert [s["index"] for s in out] == [1, 2]
    assert out[0]["text"] == "Câu một"
    assert out[1]["text"] == "Câu hai"
    assert out[1]["edited"] is True
    assert out[1]["end"] > out[1]["start"]


def test_save_then_load_roundtrip(tmp_path):
    project = tmp_path / "video1"
    raw = [
        {"start": 0.0, "end": 2.4, "text": "Câu một"},
        {"start": 2.4, "end": 5.0, "text": "Câu hai"},
    ]
    saved = save_subtitle(project, raw)
    json_path, srt_path = subtitle_paths(project)
    assert json_path.exists() and srt_path.exists()
    # subtitle.json giữ đúng cấu trúc
    data = json.loads(json_path.read_text(encoding="utf-8"))
    assert data["segments"] == saved
    # subtitle.srt đúng định dạng SRT, timestamp tăng dần
    srt_text = srt_path.read_text(encoding="utf-8")
    assert "00:00:00,000 --> 00:00:02,400" in srt_text
    assert "1\n00:00:00,000" in srt_text
    # load lại khớp
    assert load_subtitle(project) == saved


def test_load_missing_returns_empty(tmp_path):
    assert load_subtitle(tmp_path / "nope") == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_subtitle_store.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.pipeline.subtitle_store'`

- [ ] **Step 3: Write minimal implementation**

```python
# app/pipeline/subtitle_store.py
"""Lưu trữ phụ đề canonical cho Bước 1 (SRT-first).

`subtitle.json` là nguồn chính (có cấu trúc, sửa được từng dòng); `subtitle.srt`
là bản xuất ra để xem / CapCut dùng. Voice (B2) và prompt (B3) đọc từ đây.
"""

from __future__ import annotations

import json
from pathlib import Path

from app.voice.text_to_voice_cli import build_srt_from_segments


def normalize_subtitle_segments(raw: list[dict]) -> list[dict]:
    result: list[dict] = []
    index = 0
    for seg in raw or []:
        if not isinstance(seg, dict):
            continue
        text = str(seg.get("text") or "").strip()
        if not text:
            continue
        start = max(0.0, float(seg.get("start") or 0.0))
        end = float(seg.get("end") or 0.0)
        if end <= start:
            end = start + 0.05
        index += 1
        result.append(
            {
                "index": index,
                "start": round(start, 3),
                "end": round(end, 3),
                "text": text,
                "edited": bool(seg.get("edited")),
            }
        )
    return result


def subtitle_paths(project: Path) -> tuple[Path, Path]:
    base = Path(project) / "scripts"
    return base / "subtitle.json", base / "subtitle.srt"


def save_subtitle(project: Path, raw: list[dict]) -> list[dict]:
    segments = normalize_subtitle_segments(raw)
    json_path, srt_path = subtitle_paths(project)
    json_path.parent.mkdir(parents=True, exist_ok=True)
    json_path.write_text(
        json.dumps({"version": 1, "segments": segments}, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    srt_path.write_text(build_srt_from_segments(segments), encoding="utf-8")
    return segments


def load_subtitle(project: Path) -> list[dict]:
    json_path, _ = subtitle_paths(project)
    if not json_path.exists():
        return []
    try:
        data = json.loads(json_path.read_text(encoding="utf-8-sig"))
    except Exception:
        return []
    if isinstance(data, dict) and isinstance(data.get("segments"), list):
        return normalize_subtitle_segments(data["segments"])
    return []
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_subtitle_store.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add app/pipeline/subtitle_store.py tests/test_subtitle_store.py
git commit -m "feat(subtitle): canonical subtitle store (json + srt) for B1"
```

---

### Task 2: Endpoint lưu phụ đề + payload `has_subtitle`

**Files:**
- Modify: `app/web/web_server.py` (thêm request model gần `class SrtPreviewRequest` ~dòng 122; thêm endpoint sau `srt_preview` ~dòng 1416; mở rộng `_project_payload` ~dòng 466-486)
- Test: `tests/test_subtitle_payload.py`

**Interfaces:**
- Consumes: `save_subtitle`, `load_subtitle`, `subtitle_paths` từ Task 1; `_project_payload(project: Path) -> dict | None` và `runtime.require_project()` (đã tồn tại).
- Produces:
  - Endpoint `POST /api/projects/subtitle` nhận `{ "segments": list[dict] }` → ghi canonical → trả `{ "project": <payload> }`.
  - `_project_payload` trả thêm khoá `has_subtitle: bool` và `subtitle_segments: list[dict]`.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_subtitle_payload.py
from app.pipeline.subtitle_store import save_subtitle
from app.web.web_server import _project_payload


def _make_project(tmp_path):
    project = tmp_path / "video1"
    (project / "scripts").mkdir(parents=True)
    (project / "scripts" / "script_final.txt").write_text("Câu một. Câu hai.\n", encoding="utf-8")
    return project


def test_payload_without_subtitle(tmp_path):
    project = _make_project(tmp_path)
    payload = _project_payload(project)
    assert payload is not None
    assert payload["has_subtitle"] is False
    assert payload["subtitle_segments"] == []


def test_payload_with_subtitle(tmp_path):
    project = _make_project(tmp_path)
    save_subtitle(project, [{"start": 0.0, "end": 2.0, "text": "Câu một"}])
    payload = _project_payload(project)
    assert payload["has_subtitle"] is True
    assert len(payload["subtitle_segments"]) == 1
    assert payload["subtitle_segments"][0]["text"] == "Câu một"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_subtitle_payload.py -v`
Expected: FAIL — `KeyError: 'has_subtitle'`

- [ ] **Step 3: Add the import + request model**

Trong `app/web/web_server.py`, thêm import (gần các import pipeline khác ở đầu file — tìm dòng đang import từ `app.pipeline`):

```python
from app.pipeline.subtitle_store import load_subtitle, save_subtitle
```

Thêm model ngay sau `class SrtPreviewRequest(...)` (~dòng 126):

```python
class SubtitleRequest(BaseModel):
    segments: list[dict] = []
```

- [ ] **Step 4: Mở rộng `_project_payload`**

Trong `_project_payload`, ngay trước câu `return {` (~dòng 466), thêm:

```python
    subtitle_json = project / "scripts" / "subtitle.json"
    subtitle_segments = load_subtitle(project)
```

Và trong dict trả về, thêm 2 khoá (đặt cạnh `"scenes_exist"`):

```python
        "has_subtitle": subtitle_json.exists() and bool(subtitle_segments),
        "subtitle_segments": subtitle_segments,
```

- [ ] **Step 5: Thêm endpoint** (ngay sau hàm `srt_preview`, ~dòng 1416)

```python
@app.post("/api/projects/subtitle")
def save_project_subtitle(request: SubtitleRequest) -> dict[str, Any]:
    project = runtime.require_project()
    save_subtitle(project, request.segments)
    return {"project": _project_payload(project)}
```

- [ ] **Step 6: Run test to verify it passes**

Run: `python -m pytest tests/test_subtitle_payload.py -v`
Expected: PASS (2 passed)

- [ ] **Step 7: Chạy toàn bộ test backend để chắc không hồi quy**

Run: `python -m pytest tests/ -q`
Expected: tất cả PASS (không có lỗi mới so với trước).

- [ ] **Step 8: Commit**

```bash
git add app/web/web_server.py tests/test_subtitle_payload.py
git commit -m "feat(subtitle): POST /api/projects/subtitle + has_subtitle in payload"
```

---

### Task 3: Wiring frontend (App.jsx) — state phụ đề + lưu canonical

**Files:**
- Modify: `webui/src/App.jsx`

**Interfaces:**
- Consumes: payload mới `project.subtitle_segments` / `project.has_subtitle` (Task 2); `api(path, options)` helper (đã có); `saveScriptStep` (~dòng 799), `loadProjectIntoState` (~dòng 827), `createProject` (~dòng 774).
- Produces:
  - State `subtitleSegments` + setter `setSubtitleSegments` trong `App`.
  - Hàm `persistSubtitle(segments)` gọi `POST /api/projects/subtitle`.
  - Prop `subtitleSegments`, `setSubtitleSegments`, `persistSubtitle` truyền xuống `ScriptStepScreen` (dùng ở Task 4).
  - `saveScriptStep` lưu phụ đề trước khi sang B2.

- [ ] **Step 1: Thêm state phụ đề**

Trong `App()`, cạnh `const [script, setScript] = useState("")` (~dòng 322), thêm:

```jsx
  const [subtitleSegments, setSubtitleSegments] = useState([])
```

- [ ] **Step 2: Nạp phụ đề khi load/mở project**

Trong `loadState` (~dòng 398-401), trong nhánh `if (!preserveScript)`, thêm sau `setTitle(...)`:

```jsx
      setSubtitleSegments(data.project?.subtitle_segments || [])
```

Trong `loadProjectIntoState` (~dòng 833), sau `setScript(data.project.script)` thêm:

```jsx
    setSubtitleSegments(data.project.subtitle_segments || [])
```

- [ ] **Step 3: Hàm persistSubtitle**

Thêm ngay sau `persistScript` (~dòng 797):

```jsx
  // Ghi phụ đề canonical (subtitle.json + subtitle.srt). Trả project đã cập nhật.
  async function persistSubtitle(segments) {
    const data = await api("/api/projects/subtitle", { method: "POST", body: JSON.stringify({ segments }) })
    setState((current) => ({ ...current, project: data.project }))
    setSubtitleSegments(data.project.subtitle_segments || [])
    return data.project
  }
```

- [ ] **Step 4: Lưu phụ đề trong saveScriptStep**

Trong `saveScriptStep` (~dòng 799), sau `const updated = await persistScript()` thêm:

```jsx
      if (subtitleSegments.length) await persistSubtitle(subtitleSegments)
```

- [ ] **Step 5: Truyền props xuống ScriptStepScreen**

Tìm chỗ render `<ScriptStepScreen ... />` (~dòng 1325) và thêm các prop:

```jsx
            subtitleSegments={subtitleSegments}
            setSubtitleSegments={setSubtitleSegments}
            persistSubtitle={persistSubtitle}
```

- [ ] **Step 6: Verify build không lỗi**

Run: `cd webui && npm run build`
Expected: build thành công, không lỗi cú pháp/biến chưa khai báo. (Props mới chưa dùng ở Task 4 nhưng không gây lỗi.)

- [ ] **Step 7: Commit**

```bash
git add webui/src/App.jsx
git commit -m "feat(subtitle): App wiring for canonical subtitle state + persist"
```

---

### Task 4: Trình sửa SRT trong ScriptStepScreen

**Files:**
- Modify: `webui/src/App.jsx` (component `ScriptStepScreen`, ~dòng 1725-1827)
- Modify: `webui/src/styles.css` (thêm style cho input dòng SRT — tuỳ chọn, gộp vào commit này)

**Interfaces:**
- Consumes: props mới `subtitleSegments`, `setSubtitleSegments`, `persistSubtitle` (Task 3); state `srt` (preview, đã có); `formatTime` (đã import).
- Produces: UI sửa được cho từng dòng SRT (text + start/end), nút "Tách lại (ghi đè)", và đồng bộ `subtitleSegments` lên `App`.

Mô hình: khi **chưa có** phụ đề canonical (`subtitleSegments` rỗng), preview tự sinh từ `srt.segments` là nguồn hiển thị và mọi chỉnh sửa khởi tạo canonical. Khi **đã có** canonical, hiển thị/sửa trực tiếp `subtitleSegments`. Nút "Tách lại (ghi đè)" ghi đè canonical bằng preview mới nhất.

- [ ] **Step 1: Nhận props mới**

Sửa chữ ký hàm `ScriptStepScreen({ ... })` (~dòng 1725) thêm vào cuối danh sách:

```jsx
, subtitleSegments = [], setSubtitleSegments, persistSubtitle }) {
```

- [ ] **Step 2: Đồng bộ preview → canonical khi chưa có**

Ngay sau khối `useEffect` tạo preview (~dòng 1755, sau `}, [script, srtLang, srtSpeed])`), thêm: tự nạp preview vào canonical khi canonical rỗng và preview vừa đổi.

```jsx
  // Khi chưa có phụ đề canonical, preview tự động trở thành bản nháp canonical
  // để người dùng chỉnh; sau khi đã chỉnh/lưu thì canonical là nguồn chính.
  useEffect(() => {
    if (!subtitleSegments.length && srt.segments.length) {
      setSubtitleSegments?.(srt.segments.map((s, i) => ({
        index: i + 1, start: s.start, end: s.end, text: s.text, edited: false,
      })))
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [srt.segments])
```

- [ ] **Step 3: Hàm chỉnh từng dòng**

Thêm trước `return <div className="step-screen step1-screen">` (~dòng 1766):

```jsx
  const rows = subtitleSegments.length ? subtitleSegments : srt.segments.map((s, i) => ({ index: i + 1, start: s.start, end: s.end, text: s.text, edited: false }))

  const updateRow = (i, patch) => {
    const next = rows.map((row, idx) => idx === i ? { ...row, ...patch, edited: true } : row)
    setSubtitleSegments?.(next)
  }
  const deleteRow = (i) => setSubtitleSegments?.(rows.filter((_, idx) => idx !== i).map((row, idx) => ({ ...row, index: idx + 1 })))
  const addRowBelow = (i) => {
    const prev = rows[i]
    const inserted = { index: i + 2, start: prev?.end ?? 0, end: (prev?.end ?? 0) + 1, text: "", edited: true }
    const next = [...rows.slice(0, i + 1), inserted, ...rows.slice(i + 1)].map((row, idx) => ({ ...row, index: idx + 1 }))
    setSubtitleSegments?.(next)
  }
  // "Tách lại" ghi đè canonical bằng preview hiện tại (mất chỉnh tay).
  const resplitFromScript = () => {
    if (!window.confirm("Tách lại từ kịch bản sẽ ghi đè các chỉnh sửa phụ đề thủ công. Tiếp tục?")) return
    setSubtitleSegments?.(srt.segments.map((s, i) => ({ index: i + 1, start: s.start, end: s.end, text: s.text, edited: false })))
  }
```

- [ ] **Step 4: Thay khối render preview read-only bằng trình sửa**

Thay nguyên khối `<div className="srt-preview-list"> ... </div>` (~dòng 1814-1826) bằng:

```jsx
          <div className="srt-preview-list">
            {rows.length === 0
              ? <EmptyState text={script.trim() ? "Đang tạo phụ đề..." : "Nhập kịch bản bên trái để xem phụ đề SRT tự động."} />
              : rows.map((seg, i) => (
                <div className="srt-edit-row" key={i}>
                  <span className="srt-preview-idx">{i + 1}</span>
                  <div className="srt-edit-body">
                    <div className="srt-edit-time">
                      <input type="number" step="0.1" min="0" value={seg.start}
                        onChange={(e) => updateRow(i, { start: Number(e.target.value) })} />
                      <span>→</span>
                      <input type="number" step="0.1" min="0" value={seg.end}
                        onChange={(e) => updateRow(i, { end: Number(e.target.value) })} />
                      <small>{formatTime(seg.start)} → {formatTime(seg.end)}</small>
                    </div>
                    <Textarea className="srt-edit-text" value={seg.text}
                      onChange={(e) => updateRow(i, { text: e.target.value })} />
                  </div>
                  <div className="srt-edit-actions">
                    <button title="Thêm dòng dưới" onClick={() => addRowBelow(i)}><Plus className="h-3.5 w-3.5" /></button>
                    <button title="Xoá dòng" onClick={() => deleteRow(i)}><Trash2 className="h-3.5 w-3.5" /></button>
                  </div>
                </div>
              ))}
          </div>
          {rows.length > 0 && (
            <div className="srt-edit-footer">
              <Button variant="secondary" onClick={resplitFromScript}><RefreshCw className="h-4 w-4" /> Tách lại (ghi đè)</Button>
            </div>
          )}
```

- [ ] **Step 5: Style tối thiểu cho hàng sửa**

Thêm vào cuối `webui/src/styles.css`:

```css
.srt-edit-row { display:flex; gap:10px; align-items:flex-start; padding:8px 0 12px 12px; border-left:2px solid rgba(208,188,255,.25); }
.srt-edit-body { flex:1 1 auto; min-width:0; }
.srt-edit-time { display:flex; align-items:center; gap:6px; margin-bottom:6px; }
.srt-edit-time input { width:74px; background:rgba(255,255,255,.06); border:1px solid rgba(255,255,255,.12); border-radius:6px; color:#fff; padding:3px 6px; font-size:12px; }
.srt-edit-time small { color:#94a3b8; font-size:11px; }
.srt-edit-text { width:100%; min-height:44px; font-size:13px; }
.srt-edit-actions { display:flex; flex-direction:column; gap:6px; }
.srt-edit-actions button { background:rgba(255,255,255,.06); border:1px solid rgba(255,255,255,.12); border-radius:6px; color:#cbd5e1; padding:4px; cursor:pointer; }
.srt-edit-actions button:hover { color:#fff; background:rgba(255,255,255,.12); }
.srt-edit-footer { padding:8px 0 0 12px; }
```

- [ ] **Step 6: Verify build**

Run: `cd webui && npm run build`
Expected: build thành công.

- [ ] **Step 7: Manual verification (chạy app)**

Khởi động app (xem MEMORY: `run_visual_capcut_web.bat` hoặc `start_visual_capcut.ps1`). Kiểm tra:
1. Vào Bước 1, dán kịch bản nhiều câu → cột phụ đề hiện các dòng tách + timing ước tính; gõ thêm → cập nhật.
2. Sửa text 1 dòng và đổi start/end → giá trị giữ nguyên khi tiếp tục gõ ở dòng khác.
3. Bấm "Lưu & chọn giọng" → sang B2; kiểm tra file `<project>/scripts/subtitle.json` và `subtitle.srt` tồn tại, đúng nội dung đã sửa.
4. Mở lại video → trình sửa nạp đúng phụ đề canonical đã lưu (không phải bản tách lại từ script).
5. Bấm "Tách lại (ghi đè)" → xác nhận → phụ đề về bản tách tự động.
6. (Nếu đã tạo voice) sửa phụ đề → banner "cần tạo lại giọng đọc" hiện như cũ (stale).

- [ ] **Step 8: Commit**

```bash
git add webui/src/App.jsx webui/src/styles.css
git commit -m "feat(subtitle): editable SRT editor in B1 with re-split"
```

---

## Self-Review (đã rà)

- **Spec coverage:** SP1.1 mô hình dữ liệu → Task 1 + Task 2 (payload). SP1.2 thuật toán tách → tái dùng `srt_preview` sẵn có (frontend gọi, không cần task backend mới). SP1.3 API → Task 2. SP1.4 UI → Task 3 + Task 4 (live preview giữ nguyên cơ chế cũ, thêm sửa + "Tách lại" + stale banner đã có sẵn). Tiêu chí hoàn thành → Task 1/2 test + Task 4 manual verify.
- **Placeholder scan:** không có TBD/“xử lý lỗi phù hợp”; mọi step có code/lệnh cụ thể.
- **Type consistency:** segment dùng nhất quán khoá `index/start/end/text/edited`; hàm `save_subtitle`/`load_subtitle`/`normalize_subtitle_segments`/`subtitle_paths` tên khớp giữa Task 1 và Task 2; prop `subtitleSegments`/`setSubtitleSegments`/`persistSubtitle` khớp giữa Task 3 và Task 4.
- **Lưu ý:** thuật toán tách câu hiện chỉ ở backend `srt_preview`; SP1 không thêm chốt độ dài min/max (spec ghi "có thể") để giữ phạm vi tối thiểu — nếu cần sẽ làm ở vòng sau.
